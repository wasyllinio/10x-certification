package repository

// LocationRepository defines the interface for location repository
type LocationRepository interface {
}

// EVSERepository defines the interface for EVSE repository
type EVSERepository interface {
}
